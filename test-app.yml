spring:
  datasource:
    url: jdbc:postgres://localhost:5432/g4l_lms_db
    username: g4l_dev
    password: P@ssword1
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show_sql: true
      